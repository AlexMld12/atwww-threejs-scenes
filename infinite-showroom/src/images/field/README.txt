GENERATED — do not hand-edit, do not drop originals here.

These are build outputs written by `npm run images` (scripts/optimize-images.mjs)
from the full-size originals in ../../../masters/field/.

  this folder   = 512 px longest edge, WebP q78  (desktop)
  ./mobile/     = 288 px longest edge, WebP q72  (phones / <768 px)

To add or replace background images: put the originals in masters/field/ and run
`npm run images`. Both sets are regenerated with matching filenames, which is what
keeps the lane->image mapping identical on mobile and desktop.
